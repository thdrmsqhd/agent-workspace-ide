# 구현 에이전트 작업 지침

## 시작

이 저장소의 목적은 독립 Windows 에이전트 개발 IDE다. [문서 색인](docs/00-document-index.md)과 [구현 상태](docs/19-implementation-status.md)를 먼저 읽고 변경 영역의 계약을 따른다. 현재는 계약·순수 코어·SQLite 일부·Git 워크트리·Diff·파일·호스트 주입형 화면 단계다. 전체 CMD payload 검증, 완전한 DB 스키마, 실제 데스크톱 앱이나 OMP/IDE 연동이 존재한다고 가정하지 않는다.

## 우선순위와 권위

사용자 최신 지시를 최우선으로 한다. 제품 범위는 원명세와 REQ, 화면은 04/04a, 상태는 05, 데이터는 06, API는 07, 나머지 도메인은 해당 번호 문서가 권위자다. 같은 규칙을 여러 파일에 독립 정의하지 않는다. 문서 충돌은 원인과 해결을 기록한다.

## 금지하는 요구 축소

- 메인 12개를 3개 실행+9개 대기로 대체하지 않는다.
- 비선택 작업 IDE 서비스를 종료해 전환 시 재기동하지 않는다.
- diff 정렬용 시각적 빈 행을 넣지 않는다.
- 탭 닫기로 에이전트·서버를 중단하지 않는다.
- 자동 재연결에서 불명확한 부작용을 재실행하지 않는다.
- 원본 프로젝트·원래 OMP 세션을 가져오기/정리 과정에서 변경하지 않는다.

## 구현과 검증

17의 의존 순서를 따라 작업한다. 기술 후보는 검증 전 확정으로 바꾸지 않는다. 일반 코드·폴더·세부 UI 선택은 문서 기본값으로 진행하고 제품 범위를 바꾸거나 필요한 접근 권한이 없을 때만 질문한다.

주석·문서·사용자용 문구는 한국어. 코드 식별자는 영어. 실제 실행한 시험과 하지 못한 시험을 구별하고 검증 로그를 남긴다. 무관한 변경과 기존 사용자 작업 덮어쓰기 금지.

18의 명령은 구현 계약이다. 현재 실행 가능한 명령과 향후 추가할 명령을 18에서 구분한 뒤 실행한다. `npm run dev`는 앱 미구현으로 명시적으로 실패한다. PR/커밋 설명에는 관련 REQ/SCN/AT와 검증 결과를 포함한다. 토큰·비밀 설정·사용자 코드와 진단 로그를 임의로 외부 전송하지 않는다.

## 종료 보고

변경 내용·검증 결과·미완료/차단·다음 작업을 간단히 보고한다. 문서 작성, 코드 구현, 실제 연동, Windows 수용 시험, 메모리 목표 통과를 서로 다른 완료 상태로 취급한다.
