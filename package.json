{
  "name": "agent-workspace-ide",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.9.0",
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11 <12"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs:check": "node scripts/check-docs.mjs",
    "lint": "eslint .",
    "typecheck": "npm run build:foundation && npm run build:core && tsc -p packages/persistence/tsconfig.json --noEmit",
    "build:foundation": "tsc -p packages/contracts/tsconfig.json",
    "build:core": "tsc -p packages/core/tsconfig.json",
    "build:persistence": "tsc -p packages/persistence/tsconfig.json",
    "build": "npm run build:foundation && npm run build:core && npm run build:persistence",
    "test:unit": "npm run build && node --test tests/unit/*.test.mjs",
    "test:integration": "npm run build && node --test tests/integration/*.test.mjs",
    "dev": "node scripts/desktop-not-ready.mjs"
  },
  "devDependencies": {
    "@eslint/js": "9.39.1",
    "@types/node": "24.10.1",
    "eslint": "9.39.1",
    "typescript": "5.9.3",
    "typescript-eslint": "8.46.3"
  }
}
