# 에이전트 워크스페이스 IDE — 구현 명세 초안 v0.1

작성일: 2026-09-23

상태: 대화에서 확정한 제품 요구사항과 구현 제안의 통합 초안. 프로그램 구현·실행 검증·기반 저장소 선정은 아직 수행하지 않았다.

## 1. 제품 목표

여러 OMP 터미널에서 진행하던 개발을 하나의 Windows 데스크톱 프로그램에서 관리한다. 프로젝트를 선택해 요청을 구체화하고, 작업마다 독립 Git 워크트리와 메인 에이전트를 생성한다. 전체 프로젝트의 작업을 카드로 확인하고, 선택한 작업의 대화·파일·편집기·터미널·디버거로 이동한다.

핵심 성공 조건은 다음 세 가지를 동시에 만족하는 것이다.

- 평소 3개, 최대 12개 메인 작업이 실제로 동시에 코드를 수정하고 실행한다. 일부만 실행하는 대기열 방식으로 대체하지 않는다.
- VS Code 수준의 개발 기능과 폭넓은 확장 호환성을 제공한다. 선택하지 않은 작업도 자동완성·코드 분석을 유지하여 전환 때마다 재시작을 기다리지 않는다.
- 동일한 프로젝트·확장·작업량에서 현재의 VS Code + 여러 OMP 터미널 구성보다 전체 메모리 사용량을 확실히 줄인다. 아직 달성 여부는 검증되지 않았다.

표기: **확정**은 사용자 결정, **설계안**은 구현을 위한 제안, **검증 필요**는 실제 기반 기술로 확인해야 하는 조건이다.

## 2. 첫 버전 범위

| 구분 | 확정 범위 |
| --- | --- |
| 환경 | Windows 로컬 폴더, 독립 실행 데스크톱 앱 |
| 엔진 | OMP 우선, 향후 엔진 추가가 가능한 구조 |
| 프로젝트 | 여러 프로젝트 등록, 한 화면에서 통합 관리 |
| 실행 | 메인 에이전트 최대 12개 동시 작업, 하위 에이전트 개수의 고정 제한 없음 |
| 격리 | 작업별 Git 워크트리 |
| IDE | 파일 탐색·편집·검색·자동완성·선언 이동·오류 표시·리팩터링·디버깅·터미널 |
| 필수 호환 기능 | 자바 디버깅, 파이썬 자동완성, 선언으로 이동, 파일 트리 아이콘 |
| 확장 설정 | 설치한 확장은 모든 프로젝트에 공통 적용. 프로젝트별 비활성화 UI 제외 |
| 기존 작업 | 기존 OMP 대화와 선택한 변경 파일을 새 워크트리로 가져오기 |
| 실행 결과 | 외부 브라우저에서 열기 |

거의 모든 VS Code 확장을 수용하는 것이 목표이나 완전한 호환은 확정하지 않는다. 독립 앱에서 사용할 수 있는 배포 경로와 각 확장의 기술적 호환성을 확인해야 한다. 일부 확장 이용 불가는 수용하되 필수 개발 기능은 유지한다.

## 3. 대표 사용자 시나리오

### 3.1 새 작업

1. 프로젝트 목록에서 프로젝트를 선택한다.
2. 상단 입력창에 “로그인 오류를 수정하고 싶어”를 입력한다.
3. ‘준비 중’ 카드와 구체화 대화가 생성된다. 이 단계는 파일 읽기·검색만 허용하며 명령 실행·설치·테스트·코드 수정은 허용하지 않는다.
4. 사용자는 요구사항을 논의하고 엔진·모델·실행 모드·기준 브랜치를 확인한다.
5. ‘작업 시작’을 누르면 독립 브랜치와 워크트리가 생성되고 논의 내용을 받은 메인 에이전트가 실행된다.
6. 메인 카드에 현재 행동이 표시된다. 하위 에이전트가 생기면 접힌 트리 안에 추가된다.
7. 사용자는 작업 대화창에서 추가 지시를 즉시 전달하거나 대기열에 넣는다.
8. 에이전트가 완료하면 결과를 검토한다. 수동 승인 또는 지정한 자동 모드와 프로젝트 워크플로우에 따라 반영한다.

상단 입력창은 항상 새 구체화 대화용이다. 기존 작업에 대한 지시는 그 작업의 대화창에서 보낸다. 입력만으로 작업 워크트리를 만들거나 코드를 수정하지 않는다.

### 3.2 동시 작업과 전환

A는 로그인 오류 수정, B는 회원가입 추가를 수행한다. A를 열면 A의 대화와 파일·터미널이 보이고, B를 열면 B의 것으로 바뀐다. A 에이전트·서버·터미널·디버깅은 계속 유지된다. 다시 A를 열면 이전 편집 위치와 화면 배치를 복원한다.

작업 선택 때문에 코드 분석 서비스를 종료했다가 다시 켜는 절약 방식은 사용하지 않는다. 다만 초기 프로젝트 로딩이나 최초 인덱싱 시간까지 0으로 보장하는 것은 아니다.

### 3.3 응답 요청

A와 대화하는 동안 B가 질문하면 B의 카드 위치는 그대로 두고 배지·색상·알림으로 표시한다. 알림에서 간단히 답하거나 B로 이동할 수 있다. 자동으로 현재 화면을 전환하지 않는다. B만 응답을 기다리고 나머지는 계속 실행한다.

### 3.4 기존 OMP 작업 가져오기

기존 세션을 선택하고 프로젝트·기준 커밋·변경 파일을 확인한다. 가져올 변경은 기본 전체 선택이며 파일 단위로 제외할 수 있다. 새 워크트리에 선택한 변경을 옮기고 기존 대화의 문맥을 이어받는다. 원래 폴더와 원래 세션은 보존한다.

설계안: 가져오기 직전 원본 변경 스냅샷을 고정하고, 복사 중 원본이 바뀌면 차이를 알린다. 추적 파일 수정·삭제·이름 변경·새 파일을 처리한다. 무시된 파일은 자동으로 대량 복사하지 않고 필요한 항목을 명시적으로 선택하도록 한다. 이전 대화에 남아 있는 절대 경로는 새 작업 경로와 구분한다. 실패하면 부분적으로 만든 대상만 정리하고 원본은 변경하지 않는다.

검증 필요: OMP의 버전별 세션 형식과 작업 경로 재지정 방식. 단순한 세션 파일 복사만으로 안전한 이어받기가 된다고 가정하지 않는다.

## 4. 화면 구성

| 영역 | 내용과 동작 |
| --- | --- |
| 상단 | 프로젝트 선택, 새 요청 입력, 첨부 |
| 시작 화면 | 모든 프로젝트의 작업 카드. 재시작 후에도 전체 목록부터 표시 |
| 프로젝트 그룹 | 준비 중·진행 중 작업과 접을 수 있는 완료·취소 작업 |
| 메인 작업 카드 | 원래 요청, 상태, 현재 행동, 최근 추가 지시, 변경 파일 수, 마지막 활동, 하위 에이전트 수 |
| 하위 카드 | 메인 카드 아래 들여쓴 트리. 기본 접힘. 펼치면 각 하위 작업과 상태 표시 |
| 작업 상세 | 가운데 대화·진행 상황. 파일·코드·터미널은 옆이나 아래에 펼침 |
| 확장 보기 | 코드 또는 터미널을 크게 확장하고 복귀 시 기존 배치 복원 |
| 알림 | 응답 필요·실패 등. 짧은 답변 또는 해당 작업으로 이동 |

설계안: 원래 요청은 카드에서 두 줄까지 표시하고 펼쳐 전문을 확인한다. 생성된 제목이 원래 요청을 대체하지 않는다. 카드 정렬은 새 이벤트 때문에 자동으로 바뀌지 않는다.

하위 에이전트의 활동은 관찰할 수 있으나 추가 명령은 메인 에이전트에게 전달한다. 메인이 하위 작업을 조정한다.

## 5. 대화·입력 규칙

- 입력창에서 ‘즉시 전달 / 대기열에 추가’를 선택하고 Enter는 선택한 방식으로 보낸다. 기본값은 즉시 전달이며 현재 모드를 표시한다.
- 실행 전 대기 지시는 수정·삭제할 수 있다. 실행이 시작된 메시지는 과거 내용을 덮어쓰지 않고 새 지시로 정정한다.
- 이미지 붙여넣기, 파일·폴더 첨부, 편집기 선택 코드 첨부를 지원한다. 전송 전 확인·제거가 가능하다.
- 선택 코드에는 파일 경로와 선택 범위를 함께 보낸다. 설계안: 전송 당시 코드 스냅샷을 보관하여 이후 수정과 혼동하지 않게 한다.
- 폴더 첨부는 내용 전체를 무조건 프롬프트에 삽입한다는 의미가 아니다. 설계안: 경로·파일 목록을 전달하고 필요한 파일을 읽게 하며, 첨부 범위를 확인할 수 있게 한다.
- 대화 기억 사용량 및 자동 요약 알림은 별도 UI 없이 OMP에 맡긴다.
- 엔진이 지원하지 않는 이미지 입력 등은 전달 전에 설명하고 텍스트 설명 등 대안을 제공한다. 지원하지 않는 내용을 조용히 누락하지 않는다.

즉시 전달은 엔진이 지원하는 가장 빠른 지시 반영 시점에 전달한다는 뜻이다. 이미 완료된 파일 변경이나 외부 명령을 되돌리는 의미는 아니다.

## 6. 작업 제어·상태

| 사용자 상태 | 의미 |
| --- | --- |
| 준비 중 | 논의 중. 실행용 워크트리 없음 |
| 준비 작업 중 | 워크트리·세션·환경 구성 중 |
| 작업 중 | 메인 또는 하위 에이전트가 실행 중 |
| 응답 필요 | 질문·권한·사용자 판단을 기다림 |
| 재연결 중 | 엔진 연결 복구 시도 |
| 중단됨 | Esc, 중단 버튼 또는 재시작 후 사용자 재개 대기 |
| 검토 대기 | 결과가 준비되어 검토 가능 |
| 반영 대기 / 반영 중 | 같은 프로젝트의 반영 순서를 기다리거나 수행 중 |
| 충돌 해결 필요 | 반영 충돌로 해당 작업만 대기 |
| 완료 | 이번 작업의 지정 워크플로우가 완료됨 |
| 취소됨 | 실행은 중단하고 기록과 변경은 보관 |
| 실패 | 복구 시도 후 더 진행할 수 없는 오류 |

설계안: 내부적으로 실행 상태, 검토 상태, 반영 상태를 분리해 저장한다. ‘응답 필요’와 ‘서버 실행 중’ 같은 복합 상황을 단일 문자열 하나로 손실시키지 않는다.

### Esc / 중단

- 선택한 메인 작업과 하위 에이전트에 중단을 요청한다.
- 대화와 수정 내용은 보존하고 이후 지시로 재개한다.
- 다른 작업 및 별도로 띄운 개발 서버는 유지한다.
- 설계안: 중단 요청과 실제 중단 완료를 구분한다. 중단 불가능한 실행은 ‘중단 처리 중’과 남은 프로세스를 표시한다.
- 키보드 초점이 자동완성 팝업·대화상자·터미널에 있을 때는 해당 영역의 Esc 동작을 우선한다. 작업 중단 버튼은 항상 제공한다. 터미널에 입력한 Esc와 작업 중단을 혼동하지 않게 한다.

### 연결 복구

자동 재연결을 시도하고 실패 시 해당 작업만 멈추어 알린다. 설계안 기본값은 간격을 늘리는 제한적 재시도이며 횟수·시간은 구현 시 조정한다. 실행 상태 확인 없이 마지막 명령을 재전송하지 않는다. 완료 여부가 불명확한 부작용 명령은 확인 대기로 전환한다.

### 앱 종료 및 재시작

앱 종료 시 에이전트와 하위 작업을 중단하고 세션·워크트리·UI 상태를 보존한다. 다시 열면 자동 실행하지 않고 중단 상태로 복원한다. 작업 전환과 앱 종료는 다른 동작이다.

설계안: 앱이 소유한 개발 서버·터미널 프로세스도 앱 종료 시 종료한다. 외부에서 시작한 프로세스는 종료하지 않는다. 재시작 시 터미널 로그·명령 정보는 복원하지만 종료된 OS 프로세스가 살아 있는 것처럼 표시하지 않는다.

## 7. 워크트리·동시 편집·환경

- 작업은 기본적으로 저장소 기본 브랜치를 기준으로 만들고 시작 전에 변경할 수 있다. 선택한 대상 브랜치를 반영 대상으로 기록한다.
- 한 메인 작업과 하위 에이전트는 그 작업의 워크트리를 사용한다. 서로 다른 메인 작업은 별도 워크트리다.
- 환경 준비는 에이전트가 프로젝트 지침을 읽어 수행한다. 앱이 모든 프로젝트에 같은 설치·테스트 명령을 강제하지 않는다.
- 작업별 서버 포트를 배정하고 에이전트에 전달한다. 프런트엔드·백엔드처럼 복수 포트를 지원한다. 설계안: 포트 예약과 실제 바인딩 사이 충돌도 감지해 재할당한다.
- 설치·빌드 오류는 에이전트가 진단·수정·재시도한다. 해결하지 못하거나 판단이 필요하면 사용자에게 질문한다.
- 사용자가 직접 파일을 편집하면서 에이전트도 작업할 수 있다. 저장되지 않은 사용자 편집은 보존하고 외부 변경과 비교·해결할 수 있게 한다.
- 디버깅 중에도 에이전트 수정은 허용한다. 변경을 알리고 필요 시 디버깅을 재시작한다. 실행 중인 바이너리에 수정이 자동 반영된다고 보장하지 않는다.

검증 필요: 같은 워크트리의 메인·하위 에이전트가 같은 파일을 동시에 수정할 때 엔진의 충돌 처리. UI 버퍼 보호만으로 파일 시스템의 모든 동시 쓰기가 해결되지는 않는다.

설계안: Git 저장소가 아닌 폴더는 등록 후 읽기·논의는 허용하되 작업 시작 전에 Git 초기화 또는 저장소 선택을 안내한다. 사용자 모르게 저장소를 초기화하지 않는다.

## 8. 완료·반영·취소

작업마다 수동 승인과 자동 반영 모드를 선택한다. 빌드·테스트·병합·push·PR 여부는 프로젝트 워크플로우와 지침에 따른다. 테스트를 앱 공통의 완료 필수 조건으로 강제하지 않는다.

워크플로우가 없으면 코드 수정까지 수행하고 병합·push·PR은 사용자 확인을 받는다. 수동 모드에서는 결과 반영 전 승인을 받는다. 자동 모드도 워크플로우에서 허용한 범위만 수행한다.

같은 프로젝트의 반영은 한 번에 하나만 실행한다. 설계안: 저장소와 대상 브랜치별 잠금 및 반영 직전 기준 커밋 재확인을 사용한다. 충돌 작업은 별도 대기로 빼고 다른 작업을 최신 대상 브랜치 기준으로 다시 평가한다. 의존성 자동 추론·실행은 첫 버전에 넣지 않는다.

검토 화면 구성:

- 원래 요청과 추가 지시, 결과 요약
- 변경 파일과 diff
- 실행한 빌드·테스트의 결과, 수행하지 않았다면 ‘미실행’
- 실행 중인 앱 URL과 외부 브라우저 열기
- 추가 지시 / 승인 후 반영 / 작업 취소

취소는 파일 변경을 되돌리는 기능이 아니다. 메인·하위 에이전트를 중단하고 작업을 취소 상태로 보관한다. 완전 삭제는 별도 동작이며 삭제 대상과 남은 변경을 확인한다.

반영 전 추가 지시는 같은 세션·워크트리에서 이어간다. 반영 후 추가 지시는 갱신된 대상 브랜치에서 새 연결 작업을 만들고 이전 대화를 참고한다.

병합 후 미반영 변경과 실행 프로세스가 없을 때 워크트리를 자동 정리한다. 설계안: 저장되지 않은 편집 버퍼와 실행 중인 디버거도 정리 차단 조건으로 포함한다. 대화·변경 스냅샷·반영 커밋은 남긴다.

## 9. Diff — 필수 제약

**원본과 변경본의 줄을 맞추기 위한 화면상의 빈 행을 삽입하지 않는다.** 실제 파일을 변경하지 않는 시각적 공백도 금지한다.

- 좌우 각각 실제 코드의 줄 순서·공백·줄 번호를 그대로 유지한다.
- 왼쪽 삭제는 빨강, 오른쪽 추가는 초록으로 표시한다.
- 대응 변경 블록은 가운데 연결 표시로 보여준다.
- 기본은 독립 스크롤이다. 변경 블록 클릭 또는 이전·다음 변경 탐색 시 양쪽의 대응 위치로 이동한다.
- 실제 코드의 빈 줄은 그대로 보인다. 삽입·삭제만 있는 블록은 반대편의 경계 위치와 연결하며 가짜 코드 행을 만들지 않는다.

설계안: 일반 편집기 인스턴스 2개 + diff 계산 결과 + 별도 블록 연결 레이어로 구현한다. 기본 diff 컴포넌트가 삽입하는 정렬용 spacer를 그대로 쓰지 않는다. 폰트 크기·화면 크기·긴 줄에서도 가짜 행이 생기지 않는지 확인한다.

## 10. 설정과 확장

프로젝트의 기존 OMP 모델·스킬·MCP·워크플로우 설정을 기본으로 사용한다. 새 작업에서 엔진·모델·실행 모드를 변경할 수 있으며 ‘이번 작업만 적용 / 프로젝트 기본값으로 저장’을 선택한다.

설계안: 실행할 때 유효 설정의 스냅샷을 기록한다. 프로젝트 기본값 변경이 이미 실행 중인 작업의 설정을 조용히 바꾸지 않도록 한다. 인증 정보는 일반 작업 기록·로그에 복제하지 않고 엔진 인증 또는 OS 보안 저장소를 사용한다.

확장의 설치 정책은 전역 공통이다. 이는 모든 확장 프로세스를 항상 실행한다는 의미가 아니다. 언어·파일별 정상 활성화 조건은 유지한다. 프로젝트별 끄기 UI는 제공하지 않는다.

## 11. 구현 구조 제안

아래는 확정 기술 스택이 아니라 후보 구조다.

| 구성 요소 | 책임 |
| --- | --- |
| Desktop Shell | 창·알림·종료·복원, IDE 플랫폼 통합 |
| Workspace UI | 프로젝트 목록·카드 트리·대화·파일·터미널·검토 화면 |
| Task Coordinator | 작업 상태·메시지 큐·하위 관계·반영 순서 |
| Engine Adapter | OMP 세션 생성·재개·스트리밍·추가 지시·중단·이벤트 변환 |
| Worktree Service | 브랜치·워크트리 생성, 가져오기, diff 자료, 병합과 정리 |
| Process Supervisor | 에이전트·명령·서버·터미널·디버거의 소유 관계와 수명 관리 |
| IDE Services | 편집 버퍼, 언어 서비스, 확장 호스트, 디버그 연결 |
| Persistence | 프로젝트·작업·메시지·첨부·설정·이벤트·변경 기록 |

설계안: TypeScript 중심 구현, OMP는 우선 별도 프로세스 RPC로 연결한다. 엔진 충돌이 앱 전체를 종료하지 않게 하고 다른 엔진도 같은 인터페이스로 추가한다. UI 이벤트를 OMP 원시 메시지에 직접 결합하지 않는다.

서브에이전트 생성·현재 행동·중단, steer/follow-up 큐 변경, 세션 경로 재지정이 선택한 OMP 버전의 인터페이스에 실제로 노출되는지 확인해야 한다. 부족하면 명시적 엔진 확장 또는 SDK 어댑터가 필요하다. 로그 문자열 추측만으로 안정적인 상태를 만들지 않는다.

설계안: SQLite에 메타데이터·이벤트를 저장하고 큰 첨부·로그·diff 스냅샷은 파일로 저장한다. UI에는 보이는 카드·대화 구간만 렌더링한다. 파일 감시·Git 상태 조회는 중복을 줄인다. 개발 서버의 의존성 폴더를 작업 간 공유하는 최적화는 변경 격리를 깨뜨릴 수 있으므로 기본으로 적용하지 않는다.

### 데이터 모델

| 엔터티 | 주요 필드 |
| --- | --- |
| Project | id, name, repoPath, defaultBranch, engineConfigRef |
| Task | id, projectId, originalPrompt, phase, baseCommit, targetBranch, worktreePath, mode, linkedTaskId |
| AgentSession | id, taskId, parentAgentId, engine, engineSessionRef, status, currentAction |
| Message | id, taskId, role, content, deliveryMode, queueState, attachments, createdAt |
| Attachment | id, type, sourcePath, snapshotRef, selectedRange |
| Process | id, taskId, role, pid, startIdentity, ownership, status, portBindings |
| ChangeSnapshot | id, taskId, baseCommit, fileChanges, retainedContentRef |
| Integration | id, taskId, targetBranch, expectedHead, action, state, resultCommit, prUrl |
| Event | id, taskId, sequence, type, payload, timestamp |
| ViewState | taskId, tabs, cursorPositions, scrollPositions, panelLayout |

PID만으로는 같은 번호를 재사용한 다른 프로세스를 구별할 수 없으므로 시작 시각 등도 기록한다. 변경 이력의 저장이 단계별 되돌리기 기능을 의미하지는 않는다.

### 어댑터 작업과 이벤트

작업 예시: createDiscussion, startTask, importSession, sendSteer, enqueueFollowup, updateQueuedMessage, deleteQueuedMessage, abortTaskTree, resumeTask, respondToRequest, integrateTask, cancelTask.

이벤트 예시: task.stateChanged, agent.spawned, agent.activityChanged, message.delta, tool.started, tool.finished, input.required, files.changed, process.exited, integration.conflicted.

각 작업에는 요청 ID를, 각 이벤트에는 순차적으로 증가하는 번호를 부여한다. 작업 시작·반영 등의 중복 요청을 방지하고, 재연결 시 스냅샷과 미수신 이벤트로 상태를 복원한다. 엔진에서 이벤트 재전송을 지원하지 않으면 어댑터의 기록과 실제 실행 상태를 대조한다.

## 12. 기존 프로그램 재사용 판단

### 현재 결론

확인한 후보 중 전체 요구사항의 80% 이상을 충족한다고 입증된 프로그램은 없다. 추정치를 만들어 포크를 권장하지 않는다. 이번 조사는 후보의 공개 자료를 중심으로 진행했으며, 모든 프로그램을 망라한 조사나 실제 환경의 비교 시험은 아니다.

| 후보 | 재사용 가능한 부분 | 주요 부족 사항·미확인 사항 | 판단 |
| --- | --- | --- | --- |
| FaqFirebase/pi-desktop | OMP RPC, 여러 프로젝트·세션, 워크트리, 기존 이력, 터미널 | CodeMirror 기반이며 요구하는 VS Code 확장 호스트와 완전한 IDE 기능은 확인되지 않음 | 에이전트 통합의 참고 후보. 전체 포크는 아직 권장하지 않음 |
| Eclipse Theia | 독립 데스크톱 IDE 구성, 호환 확장·언어·디버깅 기능 | 본 명세의 OMP 연결·카드 UI·작업 격리·빠른 다중 워크트리 전환은 추가 구현과 검증 필요 | 기반 기술의 첫 검증 후보. 코어 포크보다 구성·확장 방식을 우선 |

Theia 채택을 확정한 것은 아니다. IDE 상태를 유지하면서 워크트리를 전환하는 구조와 메모리 절감이 어렵다면 채택 여부를 재평가한다.

### 80% 판정 방법 제안

| 평가 영역 | 가중치 |
| --- | ---: |
| IDE·필수 확장·디버깅 | 25 |
| OMP·기존 세션·대화 제어 | 20 |
| 다중 프로젝트·카드·계층 구조 | 15 |
| 워크트리·반영·워크플로우 | 15 |
| Windows·12개 동시 실행·메모리 목표 | 15 |
| diff·알림·첨부·복원 | 10 |

총 100점이다. 각 영역의 항목을 구현 완료·부분 지원·미지원·미확인으로 평가한다. 미확인 항목에는 만점을 부여하지 않는다. 80점 이상이어도 필수 IDE 기능이나 실제 동시 실행이 빠졌다면 그 한계를 밝힌다. 조건을 충족하는 후보를 확인하면 사용자 요청에 따라 포크 후 수정을 우선한다.

### 근거 자료

- Pi Desktop: https://github.com/FaqFirebase/pi-desktop
- Theia FAQ: https://theia-ide.org/docs/faq/
- Theia 앱 구성: https://theia-ide.org/docs/composing_applications/
- Theia 확장 호환성: https://theia-ide.org/docs/user_install_vscode_extensions/
- OMP RPC: https://github.com/can1357/oh-my-pi/blob/main/docs/rpc.md
- OMP SDK: https://github.com/can1357/oh-my-pi/blob/main/docs/sdk.md
- VS Code FAQ: https://code.visualstudio.com/docs/supporting/faq

독립적인 대체 제품에서 Microsoft Marketplace를 그대로 사용할 수 있다고 전제하지 않는다. Open VSX와 배포가 허용된 VSIX를 후보로 두고, 배포 출처 및 확장별 이용 조건을 확인한다. 설치 성공만으로 기능 호환성이 확보되었다고 판단하지 않는다.

## 13. 메모리 목표와 검증 조건

사용자는 ‘현재보다 확실히 적은 메모리’를 요구하면서, 선택하지 않은 작업의 IDE 서비스도 유지하기로 했다. 이 조합에서 가능한 절감 폭은 아직 확인되지 않았다. GUI를 하나로 합쳤다는 이유만으로 목표를 달성했다고 판단하지 않는다.

커뮤니티에는 Linux에서 OMP 프로세스 12개의 총 RSS가 약 3.7GB였다는 보고가 있다. 이는 Windows에서 실행할 본 제품의 측정값이 아니다. 과거 하위 에이전트를 많이 사용했을 때 발생한 메모리 누수 중에는 수정된 사례도 있으므로 현재 버전의 일반적인 성능으로 해석하지 않는다.

- 동시 실행 사례: https://github.com/can1357/oh-my-pi/issues/9916
- 과거 메모리 문제: https://github.com/can1357/oh-my-pi/issues/8003
- 관련 수정: https://github.com/can1357/oh-my-pi/pull/8004

측정 방법 제안:

1. 사용자의 실제 VS Code + OMP 구성을 기준으로 기록한다. 가상의 ‘VS Code 창 12개’를 비교 대상으로 삼지 않는다.
2. 같은 Windows PC, 저장소, 확장, OMP 버전, 대화 규모, 개발 서버 수를 사용한다.
3. 작업 3개와 12개에서 대기·편집·하위 에이전트 실행·빌드·작업 전환·장시간 사용을 측정한다.
4. 앱뿐 아니라 OMP·언어 서버·확장 호스트·터미널·빌드 등 전체 프로세스 트리를 포함한다.
5. Private Bytes와 Private Working Set 등을 구분하고 중앙값과 최대값을 기록한다. 공유 메모리의 단순 합산과 혼동하지 않는다.
6. 작업 전환 화면 표시 시간, 자동완성 응답, 메모리 누수 추세도 기록한다. 메모리가 줄어도 실사용 속도가 떨어지는 구성은 채택하지 않는다.

사용자가 지정한 수치 목표는 아직 없다. 측정 오차와 변동 폭을 넘어서는 감소를 확인해야 하며, 차이가 미미하면 ‘확실한 절감’이라고 주장하지 않는다. 12개 동시 실행이나 비선택 작업의 IDE 유지를 임의로 축소해 목표를 달성한 것으로 처리하지 않는다.

## 14. 구현 순서와 수용 기준

개별 에이전트 작업에 테스트를 강제하지 않는다. 다만 제품 자체의 작업 격리·데이터 보존·중단·동시 실행은 검증해야 한다.

| 단계 | 구현 대상 | 통과 조건 |
| --- | --- | --- |
| 0 기술 검증 | Theia 후보, OMP RPC, 필수 확장, 워크트리 2개 전환, 정렬용 빈 행 없는 diff | 자동완성·선언 이동·Java 디버깅이 동작하고 전환할 때마다 재시작하지 않음. OMP 필수 작업 지원 여부 정리 |
| 1 최소 전체 흐름 | 프로젝트 등록 → 준비 중 대화 → 작업 시작 → 워크트리 생성 → 대화·편집·터미널 | 논의 중 쓰기 금지, 실행 후 해당 워크트리 안에서 변경 수행 |
| 2 동시 작업 조작 | 다중 프로젝트·카드 트리·알림·즉시 전달/대기열·Esc | 작업 3개가 독립적으로 진행되고 한 작업의 중단·응답 대기가 다른 작업에 영향을 주지 않음 |
| 3 실사용 흐름 | 기존 세션 가져오기, 첨부, 수동/자동 반영, 충돌 처리, 이력·복원 | 원래 폴더 보존, 중복 반영 방지, 저장 전 편집 보호, 재시작 후 임의 실행 방지 |
| 4 부하 검증·배포 | 12개 동시 실행, 메모리 비교, 장시간 시험, Windows 패키지 | 동시 실행과 IDE 유지를 지키면서 메모리 목표 충족. 미달 시 기반 기술과 구조 재평가 |

추가 필수 확인 시나리오:

- 작업을 전환해도 이전 작업의 터미널·서버·디버깅 상태가 유지된다.
- 저장 전 사용자 편집과 에이전트 변경이 충돌해도 사용자 내용을 알림 없이 삭제하지 않는다.
- diff에 실제 파일에는 없는 정렬용 빈 행이 나타나지 않는다.
- 메인 작업 중단이 모든 하위 에이전트에 전달되며 개발 서버를 잘못 종료하지 않는다.
- 재연결 시 완료된 설치·커밋·push 등의 명령을 중복 실행하지 않는다.
- 앱 재시작 시 카드·이력·준비 중 대화를 복원하며 실행은 사용자가 재개한다.
- 기존 작업을 가져올 때 선택하지 않은 변경을 포함하지 않고 원본을 변경하지 않는다.
- 반영 후 워크트리가 삭제되어도 이전 diff를 열람할 수 있다.

## 15. 첫 버전에서 제외하는 기능

| 기능 | 처리 |
| --- | --- |
| A 반영 후 B를 시작하는 작업 의존 관계 | 향후 확장 기능으로 보류 |
| 앱 내부 웹 미리보기 | 향후 확장. 첫 버전은 외부 브라우저 사용 |
| 수정 단계별 되돌리기 | 현재 불필요하므로 첫 버전 제외 |
| 대화 기억 사용량·요약 알림 | 별도 표시 없이 OMP에 위임 |
| 프로젝트별 확장 비활성화 | 불필요. 모든 프로젝트에 공통 적용 |
| WSL·SSH·원격 개발 | 첫 버전 범위에서 제외 |
| OMP 이외의 엔진 | 첫 버전에는 확장 가능한 구조만 마련하고 실제 연결은 후속 구현 |

## 16. 미해결 사항과 다음 작업

추가적인 일반 취향 질문보다 다음 기술 검증을 우선한다.

1. 후보 IDE에서 여러 워크트리의 언어 서비스·확장 상태를 유지한 채 작업을 전환할 수 있는가?
2. 필수 Java·Python·아이콘 확장의 구체적인 배포 출처·버전·기능 호환성은 어떠한가?
3. OMP가 하위 에이전트 이벤트, 중단 전파, 대기 메시지 수정, 기존 세션 이동에 필요한 공개 인터페이스를 제공하는가?
4. 앱이 관리하는 단기 실행 명령과 장시간 개발 서버를 구분해 관리할 수 있는가?
5. 사용자의 현재 구성과 비교해 메모리 절감 목표를 달성할 수 있는가?

이 문서로 화면과 동작의 초기 구현에 착수할 수 있다. 다만 기반 기술 선정·확장 호환성·메모리 절감이 검증되었다는 의미는 아니다. 다음 구체적인 결과물은 0단계 검증 프로토타입과 그 결과에 근거한 기반 기술 선정이다.
